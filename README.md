# yt_chunks_embd_vertex_store
This project extracts YouTube transcripts, processes them into semantic chunks, and stores them in Google Cloud Vertex AI using vector embeddings. It ensures efficient retrieval by preventing duplicate storage through similarity checks.
